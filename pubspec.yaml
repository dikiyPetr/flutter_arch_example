name: nasa_feed
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  data:
    path: data
  domain:
    path: domain
  data_inteface_impl:
    path: data_inteface_impl
  get_it: 7.2.0
  injectable: ^1.5.0
  dio: ^4.0.4
  redux: ^5.0.0
  flutter_redux: ^0.8.2
dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.5
  flutter_lints: ^1.0.0
  injectable_generator: ^1.5.2
flutter:
  uses-material-design: true
  assets:
    - assets/apiKey.txt